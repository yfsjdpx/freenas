Security  Fixes:

o Fix an issue that can allow local user privilege escalation.
  FreeBSD SA https://www.freebsd.org/security/advisories/FreeBSD-SA-16:18.atkbd.asc

Enhancements:

o Improve the performance of AD binding.

Critical Fixes:

o Fix an issue that can cause NFS lockd to ignore locking requests
  when using multiple interfaces for NFS.

o Fix an issue that can prevent LDAP with TLS from working at
  system boot.

o Fix an issue that can cause reading of large files over AFP
  to appear corrupted to OSX clients.

Non-Critical Fixes:

o Fix a bug that causes spurious logging of a command when
  NFSv4 is being used in NFSv3 permissions mode.
